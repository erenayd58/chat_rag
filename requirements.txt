python-dotenv
sentence-transformers
# amsc-poc is the chunking library, consumed as a versioned dependency and
# pinned to an immutable commit that must already be pushed. Every
# `from amsc... import` in product code has to resolve at this revision;
# tests/unit/test_amsc_pin.py checks that against the sibling checkout's
# git history, so a stale pin fails the suite instead of the Docker build.
amsc-poc @ git+https://github.com/erenayd58/chunk.git@0d1edceb11ad8b64a37d144a0306a6b37f6c24e1
chromadb
openai
rank-bm25
nltk
numpy
ollama  # Official Ollama Python client

# Document Parsing
pymupdf  # PDF parsing (recommended)
pymupdf4llm[layout]==0.3.4  # layout-aware parsing for CHUNKER_TYPE=structure_first
faiss-cpu  # components.vectordb imports faiss unconditionally
# unstructured[pdf]  # Alternative PDF parsing (uncomment if needed)
python-docx  # DOCX parsing
Pillow  # Image handling
pytesseract  # OCR for images
markdown  # Markdown parsing
beautifulsoup4  # HTML parsing (for markdown stripping)

# Web Framework
flask
flask-cors
# The production WSGI server (python -m wsgi). Pure Python, so it needs no
# build tools in a slim image, and it runs the same on Windows and Linux --
# which matters here because the runtime is deliberately one process with a
# bounded thread pool, and gunicorn does not run on Windows at all.
waitress
