python-dotenv
sentence-transformers
# amsc-poc is the chunking library, consumed as a versioned dependency and
# pinned to an immutable commit that must already be pushed. Every
# `from amsc... import` in product code has to resolve at this revision;
# tests/unit/test_amsc_pin.py checks that against the sibling checkout's
# git history, so a stale pin fails the suite instead of the Docker build.
amsc-poc @ git+https://github.com/erenayd58/chunk.git@4820250c5b43a444d0dea02fa1d250c39d2faaa2
chromadb
openai
rank-bm25
numpy
ollama  # Official Ollama Python client

# Document Parsing
pymupdf  # PDF parsing
pymupdf4llm[layout]==0.3.4  # layout-aware parsing, the structured PDF path
python-docx  # DOCX parsing

# Web Framework
# The product contract (/api/v1) is a FastAPI application: typed request and
# response models, one exception table, and an OpenAPI document generated from
# them rather than maintained beside them. uvicorn is its host; while the
# console's Flask-era screens are still served, that application is also
# mounted inside the WSGI process (interfaces/http/coexistence.py) and waitress
# below is what actually binds the socket.
fastapi
uvicorn
flask
flask-cors
# The production WSGI server (python -m wsgi). Pure Python, so it needs no
# build tools in a slim image, and it runs the same on Windows and Linux --
# which matters here because the runtime is deliberately one process with a
# bounded thread pool, and gunicorn does not run on Windows at all.
waitress
